#include <stdlib.h>
#include <stdio.h>

int	ft_base(char *base)
{
	char *a[256];
	int i = 0;
	while (a[i])
		a[i++] = 0;
	i = 0;
	while (*base)
	{
		if (*ba)
	}
}

char	*ft_itoa_base(int nb, char *base)
{
	
}
